<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:Panor.Views;"
    x:Class="Panor.Pages.Core.Menu"
    Title="Меню"
    >
	<ContentPage.Content>
        <StackLayout Spacing="0">
            <views:AuthBlock Clicked="Handle_Clicked" Command="{Binding LoginCommand}" />
            <views:LoggedBlock Name="Александр Друздь" Email="email@gmail.com" />
            
            <ListView SeparatorVisibility="None"
                HasUnevenRows="true"
                ItemsSource="{Binding Items}"
                VerticalOptions="End"
                ItemSelected="Handle_ItemSelected"
                >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <views:MenuCell Image="{Binding Image}"
                            Text="{Binding Text}"
                            Number="{Binding Number}"
                            Command="{Binding Command}"
                            />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
	</ContentPage.Content>
</ContentPage>
